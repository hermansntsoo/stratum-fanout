[package]
build = "build.rs"
name = "stratum-fanout"
version = "0.1.0"
edition = "2021"
authors = ["Vault Labs"]
license = "MIT"
description = "Stratum fan-out — job id, nonce scan."
readme = "README.md"
repository = "https://github.com/vaultlabs/stratum-fanout"

[[bin]]
name = "stratum-fanout"
path = "src/main.rs"
